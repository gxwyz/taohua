# 安全漏洞总结报告

## 执行摘要

本次安全审计发现了 **10 个主要安全漏洞**，严重程度从"严重"到"中等"不等。必须立即修复严重和高危漏洞。

## 漏洞清单

### 🔴 严重漏洞 (立即修复)

#### 1. 硬编码的数据库密码
- **文件位置：** `admin/admin.config.php`、`web/xingcai_config.php`、`wap/xingcai_config.php`
- **问题：** 数据库密码明文存储在代码中
```php
$conf['db']['password']='dYAd4KeDY6ctczmN';  // 危险！
```
- **风险：** 如果代码泄露，攻击者可直接访问数据库
- **修复方法：** 使用环境变量，从 git 历史中删除，立即更换密码

#### 2. SQL 注入漏洞
- **文件位置：** `blast_sqlin.php`、`xingcai_sqlin.php`
- **问题：** 输入过滤不充分，可被绕过
```php
// 危险的过滤方式
$str=preg_replace("/select/i", "",$str);  // 可以用 SeLeCt 绕过
$str=preg_replace("/union/i", "",$str);   // 可以用 UNunionION 绕过
```
- **风险：** 攻击者可以窃取、修改或删除数据库中的所有数据
- **修复方法：** 使用 PDO 预处理语句

#### 3. 目录遍历漏洞
- **文件位置：** `admin/blast_back/admin/Database.class.php`
- **问题：** 文件操作未充分验证用户输入
```php
unlink($DataDir . $para['File']);  // 可以删除任意文件
```
- **风险：** 攻击者可以读取、删除服务器上的任意文件
- **修复方法：** 使用白名单验证、`basename()`、`realpath()`

### 🟠 高危漏洞 (紧急修复)

#### 4. 使用已废弃的 MySQL 函数
- **文件位置：** 多处
- **问题：** 使用 `mysql_connect()`、`mysql_query()` 等已废弃函数
```php
mysql_connect("127.0.0.1","root","password");  // PHP 7.0+ 已移除
```
- **风险：** 无法在现代 PHP 版本上运行，存在安全隐患
- **修复方法：** 迁移到 PDO 或 MySQLi

#### 5. 弱密码哈希算法
- **文件位置：** `User.class.php`、`Team.class.php`、`Member.class.php`
- **问题：** 使用 MD5 哈希密码，无盐值
```php
$password = md5($_POST['password']);  // MD5 已不安全
```
- **风险：** 密码可以被快速破解
- **修复方法：** 使用 `password_hash()` 和 `password_verify()`

### 🟡 中危漏洞 (应尽快修复)

#### 6. XSS (跨站脚本) 漏洞
- **风险：** 可能执行恶意 JavaScript
- **修复方法：** 在所有输出使用 `htmlspecialchars()`

#### 7. 缺少 CSRF 保护
- **风险：** 攻击者可以伪造请求
- **修复方法：** 实施 CSRF 令牌机制

#### 8. 会话配置不安全
- **风险：** 会话可能被劫持
- **修复方法：** 设置 `HttpOnly`、`Secure`、`SameSite` 标志

#### 9. 错误信息泄露
- **风险：** 可能泄露系统信息
- **修复方法：** 禁用错误显示，启用日志记录

#### 10. 文件上传漏洞（待确认）
- **风险：** 可能上传恶意文件
- **修复方法：** 验证文件类型，使用随机文件名

## 快速修复指南

### 修复 1: 移除硬编码密码

```php
// 错误的做法：
$conf['db']['password']='dYAd4KeDY6ctczmN';

// 正确的做法：
$conf['db']['password'] = getenv('DB_PASSWORD');
```

在服务器上设置环境变量：
```bash
export DB_PASSWORD='你的密码'
```

### 修复 2: 使用 PDO 预处理语句

```php
// 错误的做法：
$id = $_GET['id'];
$sql = "SELECT * FROM members WHERE uid=$id";
$result = mysql_query($sql);

// 正确的做法：
$stmt = $pdo->prepare("SELECT * FROM members WHERE uid = ?");
$stmt->execute([$_GET['id']]);
$result = $stmt->fetch();
```

### 修复 3: 使用安全的密码哈希

```php
// 错误的做法：
$password = md5($_POST['password']);

// 正确的做法：
// 存储密码
$hashedPassword = password_hash($_POST['password'], PASSWORD_BCRYPT, ['cost' => 12]);

// 验证密码
if (password_verify($_POST['password'], $user['password'])) {
    // 密码正确
}
```

### 修复 4: 防止目录遍历

```php
// 错误的做法：
$file = $_POST['File'];
unlink($dataDir . $file);

// 正确的做法：
$file = basename($_POST['File']);  // 移除路径部分
if (!preg_match('/^[a-zA-Z0-9_-]+\.sql$/', $file)) {
    throw new Exception("无效的文件名");
}
$fullPath = realpath($dataDir . DIRECTORY_SEPARATOR . $file);
if (!$fullPath || strpos($fullPath, $dataDir) !== 0) {
    throw new Exception("访问被拒绝");
}
unlink($fullPath);
```

### 修复 5: 添加 CSRF 保护

```php
// 生成令牌：
if (empty($_SESSION['csrf_token'])) {
    $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
}

// 在表单中：
<input type="hidden" name="csrf_token" value="<?=$_SESSION['csrf_token']?>">

// 验证令牌：
if (!hash_equals($_SESSION['csrf_token'], $_POST['csrf_token'])) {
    die('CSRF 验证失败');
}
```

### 修复 6: 防止 XSS

```php
// 在模板中输出数据时：
<?= htmlspecialchars($userInput, ENT_QUOTES, 'UTF-8') ?>
```

### 修复 7: 安全的会话配置

```php
// 在应用启动时添加：
ini_set('session.cookie_httponly', 1);
ini_set('session.cookie_secure', 1);      // 仅在使用 HTTPS 时
ini_set('session.use_strict_mode', 1);
ini_set('session.cookie_samesite', 'Strict');
```

### 修复 8: 禁用生产环境错误显示

```php
// 生产环境配置：
ini_set('display_errors', 0);
ini_set('log_errors', 1);
ini_set('error_log', '/var/log/php-errors.log');
$conf['debug']['level'] = 0;
```

## 立即行动项

### 第一步：从 Git 中移除敏感信息

```bash
# 从 Git 历史中移除配置文件
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch admin/admin.config.php web/xingcai_config.php wap/xingcai_config.php' \
  --prune-empty --tag-name-filter cat -- --all

# 强制推送（谨慎！）
git push origin --force --all
```

### 第二步：更新 .gitignore

```
# 添加到 .gitignore
*config.php
.env
*.log
/databak888/
```

### 第三步：创建配置文件模板

创建 `config.php.example`：
```php
<?php
// 配置文件示例 - 复制为 config.php 并填写实际值
$conf['db']['dsn']='mysql:host=localhost;dbname=your_database';
$conf['db']['user']='your_username';
$conf['db']['password']='your_password';
$conf['db']['charset']='utf8';
$conf['db']['prename']='blast_';
?>
```

### 第四步：立即更换所有密码

1. 更改数据库密码
2. 更新服务器配置
3. 使用环境变量或加密配置文件

## 修复优先级

### 第 1 周（紧急）：
- [ ] 移除硬编码的数据库密码
- [ ] 更换所有暴露的密码
- [ ] 修复 SQL 注入漏洞（迁移到 PDO）
- [ ] 修复目录遍历漏洞

### 第 2-3 周（高优先级）：
- [ ] 替换废弃的 MySQL 函数
- [ ] 实施安全的密码哈希
- [ ] 添加 CSRF 保护
- [ ] 进行有限的重新测试

### 第 4 周（中等优先级）：
- [ ] 改进 XSS 防护
- [ ] 加固会话管理
- [ ] 修复错误处理
- [ ] 全面安全重新测试

### 持续改进：
- [ ] 实施安全监控
- [ ] 设置自动化安全扫描
- [ ] 定期进行渗透测试
- [ ] 开发人员安全培训

## 测试清单

修复后需要验证：

### SQL 注入测试
```
在所有输入框尝试：
' OR '1'='1
1' UNION SELECT * FROM members--
```

### XSS 测试
```
在所有输入框尝试：
<script>alert(1)</script>
<img src=x onerror=alert(1)>
```

### CSRF 测试
```
从外部站点提交表单
```

### 目录遍历测试
```
在文件操作中尝试：
../../../etc/passwd
..%2F..%2F..%2Fetc%2Fpasswd
```

### 密码测试
```
检查数据库中的密码是否使用 bcrypt
检查密码长度（bcrypt 是 60 字符）
```

### 会话测试
```
在浏览器中检查 Cookie 标志：
- HttpOnly: 是
- Secure: 是（如果使用 HTTPS）
- SameSite: Strict
```

## 安全工具推荐

### 开发阶段：
- **PHPStan** - 静态代码分析
- **Psalm** - 静态分析和类型检查
- **PHPCS** - 代码风格和安全检查
- **SonarQube** - 代码质量和安全

### 测试阶段：
- **OWASP ZAP** - 自动化安全测试
- **Burp Suite** - Web 应用安全测试
- **Nikto** - Web 服务器扫描
- **SQLMap** - SQL 注入测试

### 生产阶段：
- **ModSecurity** - Web 应用防火墙
- **Fail2Ban** - 入侵防御
- **OSSEC** - 主机入侵检测
- **ELK Stack** - 日志监控

## 合规性要求

### OWASP Top 10 2021
本项目违反了以下 OWASP Top 10 项目：
- ✓ A01:2021 – 失效的访问控制
- ✓ A02:2021 – 加密机制失效
- ✓ A03:2021 – 注入
- ✓ A05:2021 – 安全配置错误
- ✓ A07:2021 – 识别和身份验证失败

### 行业标准
- **PCI DSS：** 如果处理支付信息，当前不符合要求
- **GDPR：** 数据保护措施不足
- **等保 2.0：** 需要改进安全措施

## 安全开发建议

### 代码审查
- 每次提交前进行代码审查
- 使用安全检查清单
- 培训开发人员识别安全问题

### 安全测试
- 集成自动化安全扫描到 CI/CD
- 定期进行渗透测试
- 实施漏洞赏金计划

### 监控和响应
- 实时监控异常行为
- 设置安全警报
- 制定事件响应计划

## 学习资源

### 中文资源
- **OWASP 中文项目：** https://owasp.org/www-chapter-china/
- **先知社区：** https://xz.aliyun.com/
- **FreeBuf：** https://www.freebuf.com/

### 英文资源
- **OWASP Top 10：** https://owasp.org/www-project-top-ten/
- **PHP Security：** https://www.php.net/manual/zh/security.php
- **Web Security Academy：** https://portswigger.net/web-security

## 联系方式

如有疑问或发现新的安全问题，请联系安全团队。

---

**报告日期：** 2026-02-13  
**审计范围：** 完整代码库  
**审计工具：** 手动代码审查 + 自动化扫描  
**分类：** 内部使用  
**状态：** 初步审计完成
